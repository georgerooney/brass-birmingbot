steps:
- name: 'gcr.io/kaniko-project/executor:latest'
  args:
  - --destination=${_REGISTRY}/${_IMAGE_NAME}:${_IMAGE_TAG}
  - --cache=true
  - --cache-ttl=24h
substitutions:
  _REGISTRY: us-central1-docker.pkg.dev/birmsbot/brass-rl
  _IMAGE_NAME: brass-rl
  _IMAGE_TAG: latest
